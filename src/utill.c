#include "utill.h"
extern inline double maxf(double a1, double a2);
extern inline double minf(double a1, double a2);
extern inline double signf(double n);
extern inline double clamp(double value, double lower, double upper);
extern inline unsigned posToIndex(Position pos, unsigned width);
//double maxf(double a1, double a2){
//	if(a1 > a2)
//		return a1;
//	return a2;
//}

